// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.

import { ServiceLinkerManagementContext } from "../../api/serviceLinkerManagementContext.js";
import { list } from "../../api/configurationNames/operations.js";
import { ConfigurationNamesListOptionalParams } from "../../api/configurationNames/options.js";
import { ConfigurationNameItem } from "../../models/models.js";
import { PagedAsyncIterableIterator } from "../../static-helpers/pagingHelpers.js";

/** Interface representing a ConfigurationNames operations. */
export interface ConfigurationNamesOperations {
  /** Lists the configuration names generated by Service Connector for all target, client types, auth types. */
  list: (
    options?: ConfigurationNamesListOptionalParams,
  ) => PagedAsyncIterableIterator<ConfigurationNameItem>;
}

function _getConfigurationNames(context: ServiceLinkerManagementContext) {
  return {
    list: (options?: ConfigurationNamesListOptionalParams) =>
      list(context, options),
  };
}

export function _getConfigurationNamesOperations(
  context: ServiceLinkerManagementContext,
): ConfigurationNamesOperations {
  return {
    ..._getConfigurationNames(context),
  };
}
