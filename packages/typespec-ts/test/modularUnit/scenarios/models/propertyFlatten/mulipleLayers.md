# Should not flatten property which transitive has 2-layers flattening

# Should not flatten property which has transitive at least 2-layers flattening

# Should flatten property which has non-transitive >1 layers but just single-layer flattening
