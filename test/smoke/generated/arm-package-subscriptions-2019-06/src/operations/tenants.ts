/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { PagedAsyncIterableIterator } from "@azure/core-paging";
import { Tenants } from "../operationsInterfaces";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers";
import { SubscriptionClient } from "../subscriptionClient";
import {
  TenantIdDescription,
  TenantsListResponse,
  TenantsListNextResponse
} from "../models";

/// <reference lib="esnext.asynciterable" />
/** Class containing Tenants operations. */
export class TenantsImpl implements Tenants {
  /**
   * Initialize a new instance of the class Tenants class.
   * @param client Reference to the service client
   */
  constructor(client: SubscriptionClient) {}

  /**
   * Gets the tenants for your account.
   *
   */
  public list(): PagedAsyncIterableIterator<TenantIdDescription> {
    const iter = this.listPagingAll();
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: () => {
        return this.listPagingPage();
      }
    };
  }

  private async *listPagingPage(): AsyncIterableIterator<
    TenantIdDescription[]
  > {
    let result = await this._list();
    yield result.value || [];
    let continuationToken = result.nextLink;
    while (continuationToken) {
      result = await this._listNext();
      continuationToken = result.nextLink;
      yield result.value || [];
    }
  }

  private async *listPagingAll(): AsyncIterableIterator<TenantIdDescription> {
    for await (const page of this.listPagingPage()) {
      yield* page;
    }
  }

  /**
   * Gets the tenants for your account.
   *
   */
  private _list(): Promise<TenantsListResponse> {}

  /**
   * ListNext
   *
   */
  private _listNext(): Promise<TenantsListNextResponse> {}
}
// Operation Specifications
