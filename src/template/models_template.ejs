/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 */

import * as coreHttp from "@azure/core-http";

<%_ models.forEach(function (model) { %>
/**
 * <%= model.description %>
 */
export interface <%= model.name %> {
  <%_ model.properties.forEach(function (property) { _%>
  <%_ if (!property.isConstant) { _%>
  <%_ if (property.description) { %>
  /**
   * <%= property.description %>
   */
  <%_ } _%>
  <%= property.name %><%= property.required ? '?' : '' %>: <%= property.type %><%= ';' %>
  <%_ } _%>
  <%_ }); -%>
};
<%_ }); -%>

<%_ unions.forEach(function (union) { _%>
/**
 * <%= union.description %>
 */
export type <%= union.name %> = <%- union.values.join(" | ") _%>;
<%_ }); %>
