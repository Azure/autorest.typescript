
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
/**
 * This sample demonstrates how to {{operationDescription}}
 *
 * @summary {{operationDescription}}
 */
import {
{{#if hasBody }}
  {{bodySchemaName}},
{{/if}}
  {{clientClassName}},
} from "{{clientPackageName}}";
import { DefaultAzureCredential } from "@azure/identity";

let client: {{clientClassName}};
//{{operationGroupName}}.{{operationName}}
async function {{sampleFunctionName}}() {
  {{#each methodParamAssignments}}
  {{this}}
  {{/each}}
  await client
    {{#unless isTopLevel}}
    .{{operationGroupName}}
    {{/unless}}
    .{{operationName}}({{methodParameterNames}})
    .then((res) => {
      console.log(res);
    });
}
async function main() {
  const credential = new DefaultAzureCredential();
  {{#each clientParamAssignments}}
  {{this}}
  {{/each}}
  client = new {{clientClassName}}({{clientParameterNames}});
  await {{sampleFunctionName}}();
}
main();